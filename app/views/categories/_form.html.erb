<%= form_with(model: @category) do |form| %>
  <% @category.errors.full_messages.each do |message| %>
    <div class="alert alert-danger">
      <%= message %>
    </div>
  <% end %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <%= form.hidden_field :parent_id, value: @category_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>